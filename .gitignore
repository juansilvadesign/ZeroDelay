# Build output — the publishable .zip is generated from source, not committed.
build/
dist/
web-ext-artifacts/

# Node tooling for the Firefox build (web-ext)
node_modules/

# OS / editor / cloud cruft
.DS_Store
Thumbs.db
desktop.ini
*.log
